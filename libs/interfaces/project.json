{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "interfaces",
  "prefix": "npn",
  "projectType": "library",
  "sourceRoot": "libs/interfaces/src",
  "tags": [],
  "targets": {
    "chores": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["nx run interfaces:lint:fix", "nx run interfaces:format"],
        "parallel": false
      }
    },
    "format": {
      "executor": "nx:run-commands",
      "options": {
        "command": "prettier --write \"libs/interfaces/\""
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "lint:fix": {
      "executor": "@nx/eslint:lint",
      "options": {
        "fix": true
      }
    }
  }
}
