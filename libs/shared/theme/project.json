{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "theme",
  "prefix": "npn",
  "projectType": "library",
  "sourceRoot": "libs/shared/theme/src",
  "tags": [],
  "targets": {
    "chores": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["nx run theme:lint:fix", "nx run theme:format"],
        "parallel": false
      }
    },
    "format": {
      "executor": "nx:run-commands",
      "options": {
        "command": "prettier --write \"libs/shared/theme/\""
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "lint:fix": {
      "executor": "@nx/eslint:lint",
      "options": {
        "fix": true
      }
    }
  }
}
