{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "tokens",
  "prefix": "npn",
  "projectType": "library",
  "sourceRoot": "libs/shared/tokens/src",
  "tags": [],
  "targets": {
    "chores": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["nx run tokens:lint:fix", "nx run tokens:format"],
        "parallel": false
      }
    },
    "format": {
      "executor": "nx:run-commands",
      "options": {
        "command": "prettier --write \"libs/shared/tokens/\""
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "lint:fix": {
      "executor": "@nx/eslint:lint",
      "options": {
        "fix": true
      }
    }
  }
}

