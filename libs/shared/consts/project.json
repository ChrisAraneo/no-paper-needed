{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "consts",
  "prefix": "npn",
  "projectType": "library",
  "sourceRoot": "libs/shared/consts/src",
  "tags": [],
  "targets": {
    "chores": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["nx run consts:lint:fix", "nx run consts:format"],
        "parallel": false
      }
    },
    "format": {
      "executor": "nx:run-commands",
      "options": {
        "command": "prettier --write \"libs/shared/consts/\""
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "lint:fix": {
      "executor": "@nx/eslint:lint",
      "options": {
        "fix": true
      }
    }
  }
}
