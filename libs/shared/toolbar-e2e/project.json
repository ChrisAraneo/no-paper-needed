{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "toolbar-e2e",
  "projectType": "application",
  "sourceRoot": "libs/shared/toolbar-e2e/src",
  "implicitDependencies": ["toolbar"],
  "targets": {
    "e2e": {
      "executor": "@nx/playwright:playwright",
      "options": {
        "config": "libs/shared/toolbar-e2e/playwright.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "lint:fix": {
      "executor": "@nx/eslint:lint",
      "options": {
        "fix": true
      }
    },
    "format": {
      "executor": "nx:run-commands",
      "options": {
        "command": "prettier --write \"libs/shared/toolbar-e2e/\""
      }
    },
    "chores": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "nx run toolbar-e2e:lint:fix",
          "nx run toolbar-e2e:format"
        ],
        "parallel": false
      }
    }
  }
}
