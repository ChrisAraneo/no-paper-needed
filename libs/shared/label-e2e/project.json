{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "label-e2e",
  "projectType": "application",
  "sourceRoot": "libs/shared/label-e2e/src",
  "implicitDependencies": ["label"],
  "targets": {
    "e2e": {
      "executor": "@nx/playwright:playwright",
      "options": {
        "config": "libs/shared/label-e2e/playwright.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "lint:fix": {
      "executor": "@nx/eslint:lint",
      "options": {
        "fix": true
      }
    },
    "format": {
      "executor": "nx:run-commands",
      "options": {
        "command": "prettier --write \"libs/shared/label-e2e/\""
      }
    },
    "chores": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["nx run label-e2e:lint:fix", "nx run label-e2e:format"],
        "parallel": false
      }
    }
  }
}
