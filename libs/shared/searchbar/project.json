{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "searchbar",
  "prefix": "npn",
  "projectType": "library",
  "sourceRoot": "libs/shared/searchbar/src",
  "tags": [],
  "targets": {
    "chores": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["nx run searchbar:lint:fix", "nx run searchbar:format"],
        "parallel": false
      }
    },
    "format": {
      "executor": "nx:run-commands",
      "options": {
        "command": "prettier --write \"libs/shared/searchbar/\""
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "lint:fix": {
      "executor": "@nx/eslint:lint",
      "options": {
        "fix": true
      }
    }
  }
}
