{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "note",
  "prefix": "npn",
  "projectType": "library",
  "sourceRoot": "libs/shared/note/src",
  "tags": [],
  "targets": {
    "chores": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["nx run note:lint:fix", "nx run note:format"],
        "parallel": false
      }
    },
    "format": {
      "executor": "nx:run-commands",
      "options": {
        "command": "prettier --write \"libs/shared/note/\""
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "lint:fix": {
      "executor": "@nx/eslint:lint",
      "options": {
        "fix": true
      }
    }
  }
}
