{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "locale",
  "prefix": "npn",
  "projectType": "library",
  "sourceRoot": "libs/shared/locale/src",
  "tags": [],
  "targets": {
    "chores": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["nx run locale:lint:fix", "nx run locale:format"],
        "parallel": false
      }
    },
    "format": {
      "executor": "nx:run-commands",
      "options": {
        "command": "prettier --write \"libs/shared/locale/\""
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "lint:fix": {
      "executor": "@nx/eslint:lint",
      "options": {
        "fix": true
      }
    }
  }
}
