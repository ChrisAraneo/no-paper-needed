{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "button-e2e",
  "projectType": "application",
  "sourceRoot": "libs/shared/button-e2e/src",
  "implicitDependencies": ["button"],
  "targets": {
    "e2e": {
      "executor": "@nx/playwright:playwright",
      "options": {
        "config": "libs/shared/button-e2e/playwright.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "lint:fix": {
      "executor": "@nx/eslint:lint",
      "options": {
        "fix": true
      }
    },
    "format": {
      "executor": "nx:run-commands",
      "options": {
        "command": "prettier --write \"libs/shared/button-e2e/\""
      }
    },
    "chores": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["nx run button-e2e:lint:fix", "nx run button-e2e:format"],
        "parallel": false
      }
    }
  }
}
