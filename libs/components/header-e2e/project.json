{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "header-e2e",
  "projectType": "application",
  "sourceRoot": "libs/components/header-e2e/src",
  "implicitDependencies": ["header"],
  "targets": {
    "e2e": {
      "executor": "@nx/playwright:playwright",
      "options": {
        "config": "libs/components/header-e2e/playwright.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "lint:fix": {
      "executor": "@nx/eslint:lint",
      "options": {
        "fix": true
      }
    },
    "format": {
      "executor": "nx:run-commands",
      "options": {
        "command": "prettier --write \"libs/components/header-e2e/\""
      }
    },
    "chores": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["nx run header-e2e:lint:fix", "nx run header-e2e:format"],
        "parallel": false
      }
    }
  }
}
