{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "note-e2e",
  "projectType": "application",
  "sourceRoot": "libs/components/note-e2e/src",
  "implicitDependencies": ["note"],
  "targets": {
    "e2e": {
      "executor": "@nx/playwright:playwright",
      "options": {
        "config": "libs/components/note-e2e/playwright.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "lint:fix": {
      "executor": "@nx/eslint:lint",
      "options": {
        "fix": true
      }
    },
    "format": {
      "executor": "nx:run-commands",
      "options": {
        "command": "prettier --write \"libs/components/note-e2e/\""
      }
    },
    "chores": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["nx run note-e2e:lint:fix", "nx run note-e2e:format"],
        "parallel": false
      }
    }
  }
}
